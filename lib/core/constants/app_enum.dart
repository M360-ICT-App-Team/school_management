enum FlightTicketPaymentType { full, online, partial }

enum DepositStatus { pending, approved, declined }

enum GeneralLedgerStatus { debit, credit }

enum AirportAssistanceBookingStatus { all, pending, confirmed, cancelled }

enum BookingSupportLists { pending, processing, rejected, adjusted, closed }
